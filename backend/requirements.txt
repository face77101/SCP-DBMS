# -------------------------------------------------------------------------
# 【核心大腦】網頁伺服器框架
# 負責開闢 /api/login、/api/reports/upload 等網址接口（API），
# 接收前端傳來的資料，並決定要回傳什麼網頁回應。
# -------------------------------------------------------------------------
Flask==3.0.0

# -------------------------------------------------------------------------
# 【地下管道】MySQL 資料庫連線驅動
# Python 本身聽不懂 SQL 語句。這個套件是充當 Python 與學校 MySQL 之間的翻譯官，
# 讓林乙岑可以用 Python 程式碼把 DML（例如 INSERT、SELECT）送進學校伺服器。
# -------------------------------------------------------------------------
PyMySQL==1.1.0

# -------------------------------------------------------------------------
# 【特工密碼鎖】高強度密碼雜湊套件
# 專題的安全靈魂！負責在階段二將 members.json 的明文密碼「自動加鹽雜湊」，
# 以及在階段三的 /api/login 中，自動比對前端傳來的密碼與資料庫的雜湊值是否吻合。
# -------------------------------------------------------------------------
bcrypt==4.1.2

# -------------------------------------------------------------------------
# 【隱形斗篷】環境變數讀取工具
# 負責幫你讀取本地的 `config.env` 檔案。
# 它能把 config.env 裡面的學校資料庫密碼，神不知鬼不覺地載入到 Python 的 os.environ 中，
# 確保密碼安全不外洩，是跟 Docker 的 env_file 互相配合的終極防線。
# -------------------------------------------------------------------------
python-dotenv==1.0.1